programs = [
    'access_private',
    'virtual_final',
    'fibonacci_compile_time',
    'sfinae',
    'optional_usage'
]

foreach program: programs 
    executable(program, program + '.cpp', install: true)
endforeach
